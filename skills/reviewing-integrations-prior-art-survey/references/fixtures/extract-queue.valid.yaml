# CLEAN calibration fixture — the frozen queue, wave 2.
# ONE row per admitted service, carrying every param the extract spawn takes, so a spawn never has
# to re-read a search output whose content can move after the freeze.
# Its ORDER is the contract: rows are spawned `[cursor : cursor+30]`.
schema_version: 1
meta:
  frozen_at: '2026-08-22T00:00:00Z'
  revision: 1
queue:
  - item_id: stripe.com
    title: Stripe
    id_class: host
    location: https://stripe.com
    found_by_angle: a1,a3,b2
