# Planted fixtures

Each carries EXACTLY ONE defect, and each PASSES the deterministic gate at exit 0. That is the
point: a planted defect the validator catches tests the validator, not the reviewer. These four are
shape-legal and wrong, and only a reviewer reading the evidence can say so.

**The answer key is NOT here.** It lives in the test module, so a reviewer dispatched against these
files cannot read what it is meant to find.
