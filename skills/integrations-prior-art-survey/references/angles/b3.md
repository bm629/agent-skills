# `b3` — regulated-integration constraints

## Mechanism

Read the trust and compliance surfaces of the map's `service` and `category` groups — `/.well-
known/security.txt`, a trust centre, a subprocessor list — for the constraints a regulated
integration must satisfy.

**This angle takes its input from WAVE 0 or the capability map, never from another search child.**
It discovers from the vocabulary map's own groups and enriches from its own channel. The
cross-angle join happens at two places that are not angles — the coordinator's in-memory merge and
the extract child's deep read, both wave 2.

## Axes it searches

- **`service`** — third-party services already named in the scope
- **`category`** — the integration categories the product's capabilities imply

## Sources

| row | band | `complete_listing` | yields |
| --- | --- | --- | --- |
| `vendor-trust-center` | first-party | `n/a` | One vendor's compliance posture. Measured over the regulated slice: 53.5% carry a trust surface, |
| `vendor-docs` | first-party | `n/a` | The authoritative statement of one vendor's API, auth and integration surface. |

**Fallback: `vendor-docs`.** The first-party family's terminal. A vendor with no trust centre still documents its data
handling somewhere in its own docs.

## Cap and ordering

**Cap: `25`.** BUDGET, and it binds (B3, 2026-09-03). Over the six regulated-adjacent categories — 112 rows, 86
distinct apexes — 53.5% carry a trust surface but only 20.9% carry a MACHINE-READABLE one (a
/.well-known/security.txt with a real Contact:/Expires: field). ~46 candidates against a cap of
25. b3's `enumerated` verdict reads against the second number: a trust host that only renders is
not a walk.

**Ordering: the map's own category order, then trust-center machine-readability.** Transcribe it VERBATIM into `bound.ordering`. Where the run
could not apply it, record the ordering it DID apply and say why in `ordering_deviation` — an
ordering nobody can re-apply is a cap with no honesty behind it.

## Precondition, in BOTH directions

**It holds when:** `regulatory.applies = true`.

**It does NOT hold when** none of those disjuncts is satisfied; the map records `holds: false` naming the DECIDING value, and this angle is not dispatched.

## What this angle uniquely covers

the compliance surface a regulated integration must satisfy, and whether it is machine-readable
at all.

**Required sub-topic — DATA-RESIDENCY constraints.** Folded here rather than into an always-on angle because a residency constraint on a third party is a regulatory question, and this angle is the one gated on `regulatory.applies`.

## No sibling dependency

This angle names no other angle's output anywhere above. Its mechanism reads the vocabulary map and
its own sources, so a survey missing any other angle still dispatches this one. Where a naive
reading of the taxonomy would have it consume `a1`'s candidates, the resolution is that it
DISCOVERS from wave 0 and ENRICHES from its own channel.

## Absent input

If every source above is unreachable, record each cell with its status and observable cause, set
`outcome: vacated` with the cause, and record NO candidates — recording one would mean a search
happened. If the angle was ruled out by the map, `outcome: not_run` with the map's verdict, and
nothing else.
