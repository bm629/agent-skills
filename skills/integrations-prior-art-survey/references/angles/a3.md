# `a3` — first-party integration-directory reading

## Mechanism

Read the comparable products' OWN `/integrations` directories. The comparable-product names come
from THIS type's vocabulary map (`seed-product` axis), never from the market-competitive
survey's competitor register — types are independent. web-search LOCATES a directory page and
every name it surfaces is corroborated at that page before it is recorded.

**This angle takes its input from WAVE 0 or the capability map, never from another search child.**
It discovers from the vocabulary map's own groups and enriches from its own channel. The
cross-angle join happens at two places that are not angles — the coordinator's in-memory merge and
the extract child's deep read, both wave 2.

## Axes it searches

- **`category`** — the integration categories the product's capabilities imply
- **`domain-noun`** — the domain's own vocabulary for the systems it connects TO
- **`service`** — third-party services already named in the scope
- **`seed-product`** — the comparable products whose own /integrations pages this angle reads

## Sources

| row | band | `complete_listing` | yields |
| --- | --- | --- | --- |
| `vendor-integration-pages` | first-party | `n/a` | One product's own integration directory. Measured over a ten-product sample of which FOUR are recorded by name -- atlassian 7,805, asana 563, airtable 207, monday 18; the six that yielded no count were not individually recorded, so the spread below is re-derivable and the sample's membership is not: 18 to 7,805 entries, and six of ten yielded no count at all. |
| `vendor-docs` | first-party | `n/a` | The authoritative statement of one vendor's API, auth and integration surface. |
| `merge-docs` | aggregator | `n/a` | One unified-API vendor's own provider coverage, by category. |

**Fallback: `vendor-docs`.** Resolved from the coordinator's prose "the vendor's own docs without the trust center". vendor-
docs is the first-party family's terminal and the authority for every fact.

## Cap and ordering

**Cap: `60`.** BUDGET, and the binding constraint is REACHABILITY rather than size (B3, 2026-09-03). Measured
over a ten-product sample of which FOUR are recorded by name -- atlassian 7,805, asana 563, airtable 207, monday 18; the six that yielded no count were not individually recorded, so the spread below is re-derivable and the sample's membership is not, directory sizes span 18 to 7,805 — three orders of magnitude,
so no single number is a ceiling against most of the sample — and SIX OF THE TEN yielded no
count at all (two sitemap 404s, four with the integrations path absent from a 200). 60 is a
stated budget. `bound.cap: null` transcribes a registry row that declares NO cap, and no angle here
declares one -- so a null is a `cap-matches-registry` finding rather than an option.

**Ordering: directory size descending, then seed-product name.** Transcribe it VERBATIM into `bound.ordering`. Where the run
could not apply it, record the ordering it DID apply and say why in `ordering_deviation` — an
ordering nobody can re-apply is a cap with no honesty behind it.

## Precondition, in BOTH directions

**It holds when:** `always`.

**It cannot fail to hold.** This angle is ALWAYS-ON: there is no predicate to satisfy and no classification value that rules it out. A map recording `holds: false` for it is refused by `always-on-angle-holds`.

## What this angle uniquely covers

what comparable products themselves chose to integrate with, in their own words, which no
catalog records.

## No sibling dependency

This angle names no other angle's output anywhere above. Its mechanism reads the vocabulary map and
its own sources, so a survey missing any other angle still dispatches this one. Where a naive
reading of the taxonomy would have it consume `a1`'s candidates, the resolution is that it
DISCOVERS from wave 0 and ENRICHES from its own channel.

## Absent input

If every source above is unreachable, record each cell with its status and observable cause, set
`outcome: vacated` with the cause, and record NO candidates — recording one would mean a search
happened. If the angle was ruled out by the map, `outcome: not_run` with the map's verdict, and
nothing else.
