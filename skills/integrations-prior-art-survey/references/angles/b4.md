# `b4` — unified-API abstraction reading

## Mechanism

Read the unified-API vendors' own provider lists and category slices for the abstraction layer a
complex integration surface would sit behind.

**This angle takes its input from WAVE 0 or the capability map, never from another search child.**
It discovers from the vocabulary map's own groups and enriches from its own channel. The
cross-angle join happens at two places that are not angles — the coordinator's in-memory merge and
the extract child's deep read, both wave 2.

## Axes it searches

- **`category`** — the integration categories the product's capabilities imply

## Sources

| row | band | `complete_listing` | yields |
| --- | --- | --- | --- |
| `merge-docs` | aggregator | `n/a` | One unified-API vendor's own provider coverage, by category. |
| `nango-providers` | connector-catalog | `True` | 990 providers across 31 categories, each with a docs link, an auth mode on 929 of them and — for 680 of them — a concrete vendor host derivable from authorization_url, token_url or proxy.base_url. |

**Fallback: `nango-providers`.** Resolved from the coordinator's prose "nango-providers category slices" — one row with a
qualifier, so the narrowing is the row itself. The connector-catalog family's terminal.

## Cap and ordering

**Cap: `30`.** BUDGET — sized below Nango's top-20 category median (49.5, from the coordinator's 2026-08-22 measurement of 981 providers; the 2026-09-03 re-measurement of 990 moves it only within rounding), the only
counted anchor anywhere near this angle's corpus. The unified-API vendors' own provider lists
were never counted.

**Ordering: the map's own GROUP DECLARATION ORDER -- the position of the group that PRODUCED the row -- then the provider name ascending. An earlier revision tie-broke on 'provider row order', which is a nango-providers property; merge-docs yields one unified-API vendor's coverage by category and exposes no such row order.** Transcribe it VERBATIM into `bound.ordering`. Where the run
could not apply it, record the ordering it DID apply and say why in `ordering_deviation` — an
ordering nobody can re-apply is a cap with no honesty behind it.

## Precondition, in BOTH directions

**It holds when:** `integrations.complexity = complex`.

**It does NOT hold when** that condition is not satisfied; the map records `holds: false` naming the DECIDING value, and this angle is not dispatched.

## What this angle uniquely covers

whether an abstraction layer already covers this category, which changes the build-versus-buy
answer.

## No sibling dependency

This angle names no other angle's output anywhere above. Its mechanism reads the vocabulary map and
its own sources, so a survey missing any other angle still dispatches this one. Where a naive
reading of the taxonomy would have it consume `a1`'s candidates, the resolution is that it
DISCOVERS from wave 0 and ENRICHES from its own channel.

## Absent input

If every source above is unreachable, record each cell with its status and observable cause, set
`outcome: vacated` with the cause, and record NO candidates — recording one would mean a search
happened. If the angle was ruled out by the map, `outcome: not_run` with the map's verdict, and
nothing else.
