# CLEAN calibration fixture — the frozen queue, wave 2.
# ONE row per admitted artifact, carrying every param the extract spawn takes. Its order IS the
# contract: rows are spawned in cursor slices, so a reorder re-spawns finished work.
schema_version: 1
meta:
  frozen_at: '2026-08-22T00:00:00Z'
  revision: 1
queue:
  - item_id: HF-facebook/bart-large-mnli
    title: bart-large-mnli
    id_class: model
    location: https://huggingface.co/facebook/bart-large-mnli
    found_by_angle: a1,a3
