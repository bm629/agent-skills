# `b6`

Inference-serving scale retrieval. Walks the literature and component documentation for measured serving throughput, batching behaviour and accelerator utilisation.

## Wave-0 input

`named-technology`, `load-dimension`

Its input comes from **wave 0 and its own channel only**. An angle taking its input from a sibling
angle's OUTPUT would be reading a vocabulary the sibling minted, which L-9 forbids — and
`seed_input` is the anti-drift mechanism that makes the rule checkable.

## Applicable group types

`named-technology`, `load-dimension`

The owed grid crosses these groups with this angle's own sources INTERSECTED with the map's ACTIVE
set. All three terms: dropping any one is wrong in a measurable way.

## Sources

- `arxiv-listing`
- `crossref`
- `component-docs`
- `hn-algolia`
- `usenix`

`fallback`: `crossref` — DOI metadata reaches the same preprints and papers when a listing is a recency window.

## When it runs

`precondition`: data_ml.ml_involvement != "none"

**`trigger_anchor`**: `data_ml.ml_involvement` — each a REQUIRED capability-map leaf, so the predicate cannot fail closed on a map that omits an optional field.

**`widening_legs`**: none — OPTIONAL fields only.

**`predicate_omits`**: 
- scale.data_volume — training-data volume is not serving load, and b5 owns throughput evidence.

## Cap and ordering

**`cap: 25`.** A BUDGET. Two of the five are unbounded APIs and component-docs is per-vendor.

**`ordering_signal`:** Publication date descending — this corpus moves faster than any other this type walks — then the source's listing order. Both readable on all five.

Record `bound{cap, hit, ordering, dropped_note, ordering_deviation}`. The cap is transcribed
VERBATIM from the registry — an author does not widen their own cap. `hit: true` owes a
`dropped_note` a reader could RE-APPLY: name the ordering position reached and the first row that
fell off, never "the rest were dropped".
