# `b7`

Multi-tenant and platform-scale retrieval. Walks the operational canon and the literature for isolation mechanisms, per-tenant fairness and noisy-neighbour handling.

## Wave-0 input

`system-class`, `failure-class`

Its input comes from **wave 0 and its own channel only**. An angle taking its input from a sibling
angle's OUTPUT would be reading a vocabulary the sibling minted, which L-9 forbids — and
`seed_input` is the anti-drift mechanism that makes the rule checkable.

## Applicable group types

`system-class`, `failure-class`

The owed grid crosses these groups with this angle's own sources INTERSECTED with the map's ACTIVE
set. All three terms: dropping any one is wrong in a measurable way.

## Sources

- `aws-builders-library`
- `usenix`
- `pvldb`
- `crossref`
- `azure-architecture-center`

`fallback`: `usenix` — Proceedings carry the isolation literature when a first-party essay cannot be isolated from its listing.

## When it runs

`precondition`: business.platform.type != "none" OR domain.audience in {b2b, b2b2c} OR security.authz_complexity == "multi-tenant-isolation"

**`trigger_anchor`**: `business.platform.type`, `domain.audience` — each a REQUIRED capability-map leaf, so the predicate cannot fail closed on a map that omits an optional field.

**`widening_legs`**: `security.authz_complexity` — OPTIONAL fields only.

**`predicate_omits`**: 
- scale.concurrency — concurrency is load, not tenancy; b3 and b5 own it.

## Cap and ordering

**`cap: 20`.** A BUDGET, and the corpus was measured to be UNCOUNTABLE: neither corpus its sizing names can be counted — the Builders' Library subset is not isolable and the isolation literature is unbounded under a keyword query.

**`ordering_signal`:** Independence first (peer-reviewed or third-party work outranks a provider's own account of its own isolation), then publication date descending. Both readable on all five.

Record `bound{cap, hit, ordering, dropped_note, ordering_deviation}`. The cap is transcribed
VERBATIM from the registry — an author does not widen their own cap. `hit: true` owes a
`dropped_note` a reader could RE-APPLY: name the ordering position reached and the first row that
fell off, never "the rest were dropped".

## The sizing record

**`sizing_class: budget-uncountable`.** Both named corpora are uncountable, so 20 is a budget and the independence-then-recency ordering is what decides what gets read.

- `aws-builders-library` — The multi-tenancy subset of the library is not isolable by fetch: the listing does not expose a topic filter and the essays carry no machine-readable tag.
- `usenix` — The isolation literature is unbounded under a keyword query — there is no governed set whose members could be counted.
