# A calibration fixture for the reviewing twin. It is SHAPE-LEGAL and passes the deterministic
# gate at exit 0, and something in it is wrong. What, and which numbered condition names it, is
# recorded in the test module and deliberately not here: a fixture that hints at its own answer
# turns a blind run into an open-book one. This header is byte-identical across all four.
schema_version: 1
mode: initial
version: 1
as_of: '2026-09-05'
project_band:
  concurrency: moderate
  real_time: none
  availability_target: '99'
  geo_distribution: single-region
  data_volume: large
lineage:
  extends: null
areas:
- area: primary-datastore
  default_pattern: Single-node columnar engine that spills to disk beyond the working set.
  evidence:
  - WEB-techempower-run-3#e1
  - WEB-techempower-run-3#e2
  confidence: very-low
  hard_limits:
  - limit: Working set beyond host RAM spills to disk
    source: WEB-techempower-run-3#e1
    adjustable: true
    blocks_requirement: false
  failure_modes:
  - cause_class: saturation
    evidence:
    - WEB-techempower-run-3#e1
    - WEB-techempower-run-3#e2
  migration_trigger:
    trigger: The working set exceeds host RAM, at which point scans spill to disk
    dimension: data_volume
    evidence:
    - WEB-techempower-run-3#e1
  open_gap: 'No prior art was found for the project''s own engine, so the question of how it behaves at
    this working-set ratio remains open. And the one band that does NOT carry: every figure in the backing
    round is single-client, while this project declares `concurrency: moderate`, so nothing here bears
    on concurrent behaviour.'
  currency:
    dates:
    - '2026-04-01'
    note: 'Every backing episode comes from one source published 2026-04-01, so the whole area rests on
      a single point in time on 2026 single-node hardware — NVMe, 16 cores, 64 GB. Read it as a caveat,
      not as lower confidence: the measurement is sound and it is one generation wide.'
