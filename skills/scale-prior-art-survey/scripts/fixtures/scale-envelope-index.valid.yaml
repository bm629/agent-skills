# CLEAN calibration fixture — the wave-3 index. `confidence` is the WEAKEST backing class:
# episode 2's `confidence` is `very-low`, so the area's is too. Re-derived, never averaged.
schema_version: 1
version: 1
as_of: '2026-09-05'
project_band:
  concurrency: moderate
  real_time: none
  availability_target: '99'
  geo_distribution: single-region
  data_volume: large
lineage:
  extends: null
areas:
- area: primary-datastore
  default_pattern: Single-node columnar engine that spills to disk beyond the working set.
  evidence:
  - WEB-techempower-run-3#e1
  - WEB-techempower-run-3#e2
  confidence: very-low
  hard_limits:
  - limit: Working set beyond host RAM spills to disk
    source: WEB-techempower-run-3#e1
    adjustable: true
    blocks_requirement: false
  failure_modes:
  - cause_class: saturation
    evidence:
    - WEB-techempower-run-3#e1
    - WEB-techempower-run-3#e2
  migration_trigger:
    trigger: The working set exceeds host RAM, at which point scans spill to disk
    dimension: data_volume
    evidence:
    - WEB-techempower-run-3#e1
  open_gap: 'The project''s own ENGINE is unevidenced. b5 walked `tpc-results`, `spec-results`, `techempower`
    and `clickbench` for `duckdb` and returned zero from all four; every one is `complete_listing: true`,
    so those are four complete walks that did not find it. What the corpus evidences is the CLASS of engine,
    not this one. The remaining question is the working-set RATIO this project runs at, which no prior-art
    episode could answer -- it is a scope fact. And the one band that does NOT carry: every figure in
    the backing round is single-client, while this project declares `concurrency: moderate`, so nothing
    here bears on concurrent behaviour and no episode was found that does.'
